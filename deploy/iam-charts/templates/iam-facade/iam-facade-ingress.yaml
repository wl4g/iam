{{- if .Values.iam-facade.ingress.api.enabled -}}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}

kind: Ingress
metadata:
  name: {{ printf "%s-%s" (include "iam.fullname" .) "dashboard" }}
  labels:
    app.kubernetes.io/name: {{ include "iam.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "iam.chart" . }}
  {{- if .Values.iam-facade.ingress.api.annotations }}
  annotations:
    {{- toYaml .Values.iam-facade.ingress.api.annotations | nindent 4 }}
  {{- end }}

spec:
{{- if and .Values.iam-facade.ingress.api.ingressClassName (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.iam-facade.ingress.api.ingressClassName }}
{{- end }}

  rules:
  {{- range $host := .Values.iam-facade.ingress.api.hosts }}
  - host: {{ $host }}
    http:
      paths:
      - path: /
        {{- if (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
        pathType: ImplementationSpecific
        {{- end }}
        backend:
          {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
          service:
            name: {{ include "iam.fullname" $ }}
            port:
              number: {{ $.Values.iam-facade.service.apiPort }}
          {{- else }}
          serviceName: {{ include "iam.fullname" $ }}
          servicePort: {{ $.Values.iam-facade.service.apiPort }}
          {{- end }}
  {{- end -}}
  {{- if .Values.iam-facade.ingress.api.tls }}
  tls:
    {{- toYaml .Values.iam-facade.ingress.api.tls | nindent 4 }}
  {{- end }}
---
{{- end }}

{{- if .Values.iam-facade.ingress.prometheus.enabled -}}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}

kind: Ingress
metadata:
  name: {{ printf "%s-%s" (include "iam.fullname" .) "prometheus" }}
  labels:
    app.kubernetes.io/name: {{ include "iam.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "iam.chart" . }}
  {{- if .Values.iam-facade.ingress.prometheus.annotations }}
  annotations:
    {{- toYaml .Values.iam-facade.ingress.prometheus.annotations | nindent 4 }}
  {{- end }}

spec:
{{- if and .Values.iam-facade.ingress.prometheus.ingressClassName (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.iam-facade.ingress.prometheus.ingressClassName }}
{{- end }}
  rules:
  {{- range $host := .Values.iam-facade.ingress.prometheus.hosts }}
  - host: {{ $host }}
    http:
      paths:
      - path: /
        {{- if (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
        pathType: ImplementationSpecific
        {{- end }}
        backend:
          {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
          service:
            name: {{ include "iam.fullname" $ }}
            port:
              number: {{ $.Values.iam-facade.service.prometheusPort }}
          {{- else }}
          serviceName: {{ include "iam.fullname" $ }}
          servicePort: {{ $.Values.iam-facade.service.prometheusPort }}
          {{- end }}
  {{- end -}}
  {{- if .Values.iam-facade.ingress.prometheus.tls }}
  tls:
    {{- toYaml .Values.iam-facade.ingress.prometheus.tls | nindent 4 }}
  {{- end }}
---
{{- end }}
